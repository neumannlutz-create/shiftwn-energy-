# ShiftWN Hedging - Requirements
# ================================

# Core (already included - no install needed)
numpy>=1.24.0

# Data Sources (install for live data)
yfinance>=0.2.30          # Yahoo Finance: BDI proxy, Brent, WTI
investpy>=1.0.1           # Investing.com: Baltic indices
fredapi>=0.5.1            # FRED: shipping macro data

# Optional: Real bunker prices (requires API key)
# shipandbunker              # Ship & Bunker API (commercial)
# clarksons-api              # Clarksons Research (commercial)

# ML Backup (install for LSTM)
# torch>=2.0                 # PyTorch LSTM
# scikit-learn>=1.3          # Preprocessing, evaluation
# statsmodels>=0.14          # ARIMA, seasonal decomposition

# Dashboard (install for web UI)
# streamlit>=1.30            # Interactive dashboard
# plotly>=5.18               # Candlestick charts
# dash>=2.14                 # Alternative dashboard

# Broker Integration (install for live trading)
# ccxt>=4.0                  # Crypto/commodity exchanges
# ibapi>=10.19               # Interactive Brokers
# ib_insync>=0.9             # IB async wrapper

# Notifications
# python-telegram-bot>=20    # Telegram alerts
# sendgrid>=6.10             # Email alerts
# twilio>=8.10               # SMS alerts
